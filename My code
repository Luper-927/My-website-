<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Vibely - Powered by Luper</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #111;
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      padding-bottom: 100px;
    }
    header {
      background: #222;
      padding: 20px;
      width: 100%;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
    }
    .video-feed {
      width: 100%;
      max-width: 400px;
      margin-top: 20px;
      position: relative;
    }
    video {
      width: 100%;
      border-radius: 12px;
    }
    .controls {
      margin-top: 20px;
      display: flex;
      gap: 15px;
      justify-content: center;
    }
    .controls button {
      background: #00b894;
      border: none;
      padding: 12px 20px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      color: white;
    }
    .chatbot {
      position: fixed;
      bottom: 20px;
      right: 20px;
      width: 300px;
      background: #1e1e1e;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.6);
      display: none;
      flex-direction: column;
    }
    .chatbot-header {
      background: #333;
      padding: 10px;
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
      font-weight: bold;
    }
    .chatbot-messages {
      height: 200px;
      overflow-y: auto;
      padding: 10px;
      font-size: 14px;
    }
    .chatbot-input {
      display: flex;
      border-top: 1px solid #444;
    }
    .chatbot-input input {
      flex: 1;
      padding: 10px;
      border: none;
      background: #2a2a2a;
      color: white;
    }
    .chatbot-input button {
      background: #00b894;
      border: none;
      color: white;
      padding: 10px 15px;
      cursor: pointer;
    }
    footer {
      position: fixed;
      bottom: 0;
      background: #000;
      width: 100%;
      text-align: center;
      padding: 10px;
      font-size: 12px;
    }
  </style>
</head>
<body>

  <header>Vibely App by Luper</header>

  <div class="video-feed">
    <video controls autoplay loop muted>
      <source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4" />
      Your browser does not support the video tag.
    </video>
  </div>

  <div class="controls">
    <button onclick="toggleChat()">💬 Chat</button>
    <button onclick="alert('Marketplace is coming soon')">🛒 Marketplace</button>
    <button onclick="alert('Profile page coming soon')">👤 Profile</button>
  </div>

  <div class="chatbot" id="chatBot">
    <div class="chatbot-header">🤖 ChatGPT Assistant</div>
    <div class="chatbot-messages" id="chatMessages">
      <div>User: Hello</div>
      <div>Bot: Hi Luper! How can I help you today?</div>
    </div>
    <div class="chatbot-input">
      <input type="text" id="chatInput" placeholder="Ask me anything...">
      <button onclick="sendMessage()">Send</button>
    </div>
  </div>

  <footer>&copy; 2025 Abenga David Luper. Vibely Project.</footer>

  <script>
    function toggleChat() {
      const chat = document.getElementById("chatBot");
      chat.style.display = chat.style.display === "flex" ? "none" : "flex";
    }

    function sendMessage() {
      const input = document.getElementById("chatInput");
      const msgBox = document.getElementById("chatMessages");
      if (input.value.trim() !== "") {
        msgBox.innerHTML += `<div>User: ${input.value}</div>`;
        msgBox.innerHTML += `<div>Bot: This is a sample response for "${input.value}"</div>`;
        input.value = "";
        msgBox.scrollTop = msgBox.scrollHeight;
      }
    }
  </script>

</body>
</html>
